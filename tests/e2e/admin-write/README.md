# Admin Write Tests

Admin mutation flows belong here: session adjustment, cancellation, refunds, manual sessions, settings writes, and spot overrides.

Use `ui/` for browser-driven admin tests. API-level command tests can live beside it when they do not need a browser.
